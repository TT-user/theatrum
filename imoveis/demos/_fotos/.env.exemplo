# Copie para .env (sem o .exemplo) e cole a chave.
# O .gitignore da raiz barra o .env: este repositório é publicado como
# site, e uma chave commitada aqui fica servida em texto puro no domínio.
#
# A chave sai de https://aistudio.google.com/apikey
GEMINI_API_KEY=
